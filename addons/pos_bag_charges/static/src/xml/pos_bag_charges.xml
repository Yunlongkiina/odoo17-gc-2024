<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="pos_bag_charges.PosBagButton">
        <!--
        <div class="control-button customer-button btn rounded-0 fw-bolder text-truncate" t-attf-class="{{this.partner ? 'btn-primary' : 'btn-light'}}"
         t-on-click="() => this.onClick()">
        -->
        <div class="control-button btn btn-light rounded-0 fw-bolder sh_ctrl_btn" t-on-click="() => this.onClick()">
            <span class="fa fa-shopping-bag" aria-hidden="true"></span>
            <span>Bags</span>
        </div>
    </t>
</templates>